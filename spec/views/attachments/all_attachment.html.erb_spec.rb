# frozen_string_literal: true

require 'rails_helper'

describe 'attachments/all_attachment.html.erb', type: :view do
  it 'show all attachments view' do
    #  user = create(:user)
    #  attachment = create(:attachment, user: user)
    #  assigns[:@users]
    #  render 'attachments/all_attachments.html.erb', locals: { attachments: attachments }
    #  expect(rendered).to match /text/
  end
end
